package com.example.students;

public class Test {
    public void UnitTest(){
        /*StudentManager manager = new StudentManager(new MockRepo());
        StudentDataService service = new StudentDataService(manager);

        service.getAllStudents();*/
    }
}
